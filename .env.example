# Server-side only. Never expose these in HTML or client-side JavaScript.
# Required by /api/visitors to read the public visitor counter from PostHog.
POSTHOG_PERSONAL_API_KEY=your_private_posthog_key

# Optional: defaults to the project id hardcoded in api/visitors.js.
POSTHOG_PROJECT_ID=your_posthog_project_id
